<!-- created by Bailey-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="cse110.com.cse110_chores.ChoresScreen">

    <!-- layout for the chores screen-->
    <LinearLayout
        android:id="@+id/chorelayout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content">

        <!-- for user to input a chore name-->
        <EditText
            android:id="@+id/chore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_weight="2"
            android:singleLine="true"
            android:hint="@string/chore" />

        <!-- spinner for the user to choose a frequency-->
        <Spinner
            android:id="@+id/choreSpinner"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:spinnerMode="dropdown"
            android:layout_weight="1">
        </Spinner>

    </LinearLayout>

    <!-- button to offically add the chore into the chore list-->
    <Button
        android:id="@+id/addChore"
        android:layout_alignParentBottom="true"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/choreAddButton"
        android:gravity="center"/>

</RelativeLayout>
